export const metadata = {
    title: 'Portfolio Management | Dawlogger',
};

import PortfolioClient from './PortfolioClient';

export default function PortfolioManagementPage() {
    return <PortfolioClient />;
}
