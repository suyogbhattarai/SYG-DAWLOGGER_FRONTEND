module.exports = {
  theme: {
    extend: {
      colors: {
        'brand': {
          primary: '#00d2ff', // Cyan
          secondary: '#3a7bd5', // Blue
          dark: '#050510',    // Deep Dark
          navy: '#0b1021',    // Keep for legacy reference if needed, but updated to deeper tone
          coral: '#00d2ff',   // Mapping coral to primary for instant switch, or keep as legacy
        }
      },
    },
  },
} 